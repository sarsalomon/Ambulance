import { observer } from 'mobx-react-lite';
import React from 'react';

const ShiftView = observer(() => {
    return (
        <div>
            2
        </div>
    );
});

export default ShiftView;