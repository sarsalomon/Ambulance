import React, { useContext, useEffect, useState } from 'react';
import { observer } from 'mobx-react-lite';

const ShiftAdd = observer(() => {
    return (
        <div>
            2
        </div>
    );
});

export default ShiftAdd;